<Window x:Class="CourseProject.UI.CardDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:CourseProject.UI.Converters"
        mc:Ignorable="d"
        FontSize="12"
        Title="Карточка кота — просмотр" Height="500" Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <converters:PhotoPathToImageSourceConverter x:Key="PhotoPathToImageSourceConverter" />
    </Window.Resources>
    <Grid Margin="16">
        <Grid.Background>
            <ImageBrush ImageSource="/Images/flowers.png"/>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Image Grid.Row="0"
               Source="{Binding PhotoPath, Converter={StaticResource PhotoPathToImageSourceConverter}}"
               Width="170" Height="80"
               HorizontalAlignment="Center"
               Stretch="Uniform"
               Margin="0,0,0,10" />

        <TextBlock Grid.Row="1"
                   Text="{Binding Name}"
                   FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10" />

        <Border Grid.Row="2" Padding="0,5" Background="#90FFFFFF">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,3">
                    <TextBlock Text="Порода:" FontWeight="Bold" Width="130" />
                    <TextBlock Text="{Binding Breed}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,3">
                    <TextBlock Text="Окрас:" FontWeight="Bold" Width="130" />
                    <TextBlock Text="{Binding Color}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,3">
                    <TextBlock Text="Дата рождения:" FontWeight="Bold" Width="130" />
                    <TextBlock Text="{Binding DateOfBirth, StringFormat=dd.MM.yyyy}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,3">
                    <TextBlock Text="Вес (кг):" FontWeight="Bold" Width="130" />
                    <TextBlock Text="{Binding Weight}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,3">
                    <TextBlock Text="Цвет глаз:" FontWeight="Bold" Width="130" />
                    <TextBlock Text="{Binding EyeColor}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,3">
                    <TextBlock Text="Пол:" FontWeight="Bold" Width="130" />
                    <TextBlock Text="{Binding GenderText}" />
                </StackPanel>

                <Separator Margin="0,10" />

                <CheckBox Content="Привит" IsChecked="{Binding IsVaccinated, Mode=OneWay}" IsHitTestVisible="False" Margin="0,3" />
                <CheckBox Content="Стерилизована/Кастрирован" IsChecked="{Binding IsSterilized, Mode=OneWay}" IsHitTestVisible="False" Margin="0,3" />
                <CheckBox Content="Есть документы" IsChecked="{Binding HasDocuments, Mode=OneWay}" IsHitTestVisible="False" Margin="0,3" />
            </StackPanel>
        </Border>

        <Button Grid.Row="3"
                Content="Закрыть"
                HorizontalAlignment="Right"
                Padding="10,4"
                Margin="0,15,0,0"
                Click="ButtonClose_Click" />
    </Grid>
</Window>
