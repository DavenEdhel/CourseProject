<Window x:Class="CourseProject.UI.CardDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:CourseProject.UI.Converters"
        mc:Ignorable="d"
        FontSize="12"
        Background="{StaticResource WindowBackgroundBrush}"
        Title="Питомник — Просмотр карточки" Height="500" Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <converters:PhotoPathToImageSourceConverter x:Key="PhotoPathToImageSourceConverter" />
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Image Grid.Row="0"
               Source="{Binding PhotoPath, Converter={StaticResource PhotoPathToImageSourceConverter}}"
               Width="170" Height="80"
               HorizontalAlignment="Center"
               Stretch="Uniform"
               Margin="0,0,0,10" />

        <TextBlock Grid.Row="1"
                   Text="{Binding Name}"
                   Style="{StaticResource HeadingTextStyle}"
                   Margin="0,0,0,10" />

        <Border Grid.Row="2" Padding="0,5" Background="{StaticResource PanelOverlayBrush}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,3">
                    <Label Content="Порода:" Padding="0" Width="130" />
                    <TextBlock Text="{Binding Breed}" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,3">
                    <Label Content="Окрас:" Padding="0" Width="130" />
                    <TextBlock Text="{Binding Color}" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,3">
                    <Label Content="Дата рождения:" Padding="0" Width="130" />
                    <TextBlock Text="{Binding DateOfBirth, StringFormat=dd.MM.yyyy}" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,3">
                    <Label Content="Вес (кг):" Padding="0" Width="130" />
                    <TextBlock Text="{Binding Weight}" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,3">
                    <Label Content="Цвет глаз:" Padding="0" Width="130" />
                    <TextBlock Text="{Binding EyeColor}" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,3">
                    <Label Content="Пол:" Padding="0" Width="130" />
                    <TextBlock Text="{Binding GenderText}" VerticalAlignment="Center" />
                </StackPanel>

                <Separator Margin="0,10" />

                <CheckBox Content="Привит" IsChecked="{Binding IsVaccinated, Mode=OneWay}" IsHitTestVisible="False" Margin="0,3" />
                <CheckBox Content="Стерилизована/Кастрирован" IsChecked="{Binding IsSterilized, Mode=OneWay}" IsHitTestVisible="False" Margin="0,3" />
                <CheckBox Content="Есть документы" IsChecked="{Binding HasDocuments, Mode=OneWay}" IsHitTestVisible="False" Margin="0,3" />
            </StackPanel>
        </Border>

        <Button Grid.Row="3"
                Content="Закрыть"
                HorizontalAlignment="Right"
                Style="{StaticResource SecondaryButtonStyle}"
                Margin="0,15,0,0"
                Click="ButtonClose_Click" />
    </Grid>
</Window>
